# 메뉴판(Enterprise 고객 상품 제안) — 인수인계

> 이 문서는 변경 이력이 아니라 **현재 상태와 작업 규칙**을 기록한다. 새 작업은 아래 `최근 변경`에 한 항목으로만 추가하고, 이후 상태에 흡수되면 통합한다.

## 0. 대원칙

- 본 프로젝트의 요청에 따른 작업사항은 모두 이 문서에 입력하여, 언제 누가 프로젝트를 이어받아도 일관성 있게 진행될 수 있도록 한다.
- 현재 메인 기준은 `mockup_v2/mockup_v2.html`이다. 이 문서의 기존 1~7절에 나오는 구버전 파일 경로는 `legacy/` 아래로 이동된 레거시 체인을 설명한다.

## 1. 목적과 산출물

- 고정된 Key Account 53개사의 상품 사용·매출을 보여주는 단일 HTML 메뉴판이다. 서버 없이 브라우저에서 실행하며, CSV 업로드로 데이터를 교체한다.
- [mockup.html](mockup.html): 데모/시연용. 결정론적 합성 데이터 53개사·598행을 내장한다.
- [mockup-vdi.html](mockup-vdi.html): VDI 배포용. `RAW=[]` 상태에서 CSV 업로드로 실데이터를 읽는다.
- 두 HTML은 **생성 산출물**이다. 직접 수정하지 말고 아래 소스를 수정한 뒤 반드시 빌드한다.

```bash
node build.mjs
node --check build.mjs
node --check verify_ui.mjs
node --check verify_tax.mjs
```

## 2. 소스 구조와 빌드 규칙

- [build.mjs](build.mjs): 조립기. `CATALOG`·`INDUSTRIES`와 시드 기반 RNG로 합성 데이터를 결정론적으로 생성한다. `Math.random()`은 사용하지 않는다.
- [.base_css.txt](.base_css.txt): 공통 CSS(디자인 토큰·테이블·배지·리사이저 등).
- [.tabx.css](.tabx.css): 메뉴판 화면 CSS.
- [.tabB.html](.tabB.html): 메뉴판 마크업.
- [.tabB.js](.tabB.js): 메뉴판 렌더링·이벤트·비교 화면 로직.
- [.surv.txt](.surv.txt): 공용 집계·추천·CSV 파서 로직.

`node build.mjs`는 위 조각을 합쳐 `mockup.html`과 `mockup-vdi.html`을 함께 갱신한다. 별도 최소 글자 크기 파일은 더 이상 빌드하지 않는다. 과거 실험/변형본은 [mockup_variation](mockup_variation/)에 있으며, 현재 기준 파일이 아니다.

## 3. 현재 화면과 UX 결정

### 공통

- 분류 기준은 **AX 구분만** 사용한다: `AX/Legacy` 대분류 → `Infra/Application` 중분류.
- AI Stack은 모든 화면·토글·상품분류체계 팝업에서 제거됐다.
- 모든 화면의 최소 글자 크기는 **12px**이다. 보조 텍스트·배지·`small`도 12px 미만으로 낮추지 않는다.
- 디자인 토큰은 `.base_css.txt`의 `--u-*`를 사용한다. 브랜드 강조색은 `--u-primary: #b90064`, 히트맵/커버리지 농도는 `rgba(155,9,80, α)`다.
- 상세 화면의 좌상단 라벨과 뒤로가기/제목 사이 간격은 14px로 통일한다.

### 탭 B — 작업중 메뉴판(v0.6)

- 메인 안내 문구: `Key Account 상품 침투율을 전체 관점과 고객/상품 관점으로 제공합니다.`
- 상품별 현황: AX 계층 드릴다운 커버리지 표. 커버리지 막대는 고객별 히트맵과 동일한 색상·농도 규칙(`0.28 + 사용률 × 1.1`, 최대 0.98)을 쓴다.
- 고객별 현황: 고객 × AX 중분류 히트맵, 서비스 수/매출 기준 전환, 필터, 페이지네이션, 전체 펼치기를 제공한다. AX 2단 헤더는 14px·8px padding이다.
  - 업종 그룹 정렬은 `INDUSTRY_ORDER`(`build.mjs`) 기준이며, 실 데이터 업종값이 이 목록에 없으면 순위가 모두 동률(99) 처리돼 그룹이 매출/서비스 수 순서로 흩어져 보인다. 실 업종을 추가/변경할 때는 `build.mjs`의 `INDUSTRY_ORDER`와 합성 데이터 `INDUSTRIES`를 함께 갱신한다.
  - 현재 `INDUSTRY_ORDER = ['금융','대기업','온라인/IT','글로벌','공공','SME']`. 그룹 헤더에는 `업종명(해당 업종 고객수)`를 표시한다(`.tabB.js`의 `industryCounts`, 페이지네이션과 무관하게 필터링된 전체 기준).
  - 정렬 2차 키는 토글 기준에 따라 달라진다: `서비스 수 기준`이면 `usedTotal` 내림차순, `매출 기준`이면 `salesTotal` 내림차순(`.tabB.js:140`).
  - `.bc-wrap`의 세로 높이는 `calc(100vh - 260px)`로 화면 높이에 맞춰 확장돼 있다(고정 640px 아님).
- 고객 상세: 사용/미사용/추천 KPI와 AX 대분류별 상품 목록을 제공한다. `동종 업종과 비교`는 기본 버튼, `동종 고객과 비교`만 버건디 강조 버튼이다.
- 동종 고객 비교: 2~4개 고객을 비교한다. 공통 보유는 청록 음영, 일부 보유는 버건디 음영, 기준 고객 헤더는 버건디로 강조한다. 카테고리/소계는 `AX · Infra`처럼 대분류를 포함해 표기한다.
- 동종 업종 비교: 기준 고객이 속한 업종의 전체 고객을 대상으로, **모든 상품**을 `상품 | 업종공통 | 기준 고객` 3열로 표시한다. 업종 내 아무도 보유하지 않은 상품도 포함한다. 업종공통 0명과 기준 고객 미침투는 모두 같은 버건디 `미침투` 배지로 보인다. 업종공통은 `보유 고객 수/업종 전체 고객 수` 뒤에 `(백분율%)`를 함께 표시한다(`.tabB.js:246`).
- 상품분류체계 팝업: 별도 업로드 없이 `RAW`(매출데이터)를 `AX구분_대/AX구분_중/중분류/매출명/고객소통명` 기준으로 피벗해 만든 `TAXO`를 렌더링한다(`pivotTaxo()`, `.surv.txt`). 컬럼은 `AX구분(대)/AX구분(중)/중분류/매출명/고객소통명` 5열이다(5절 참조). 매출데이터에서 직접 뽑으므로 모든 행이 정의상 매출이 존재해, 과거의 `KA여부`(매출발생) 체크 컬럼과 손익명 유사도 매칭은 더 이상 필요 없어 제거했다.

## 4. 데이터·계산 규칙

- 커버리지는 시장 침투율이 아니라 **Key Account 53개사 중 사용 고객 비율**이다.
- 2025는 연간, 2026은 YTD(1~6월)다. 기간 길이가 달라 YoY 증감률은 표시하지 않는다.
- 화이트스페이스 기준은 커버리지 40% 미만이다. 관련 하드코딩은 `.tabB.js`의 `wsSvcCount()`·`renderNode()`와 화면 문구에 있으므로 변경 시 함께 점검한다.
- 서비스 사용 여부는 선택 기간의 해당 서비스 매출 합계가 0보다 큰지로 판단한다.

### 추천 로직 (`.surv.txt`의 `recommended()`)

대상 고객의 **미사용 상품**만 후보로 하며, 같은 AX 중분류 안에서 최대 3개를 순차 선택한다.

1. 동종 업종 침투율(`peerCount`)
2. 동종 업종 평균매출(`peerSales / peerCount`)
3. 타 업종 침투율
4. 타 업종 평균매출

- 각 단계는 남은 후보 중 해당 지표의 최고값 동률군부터 채운다.
- 대상 고객이 이미 보유한 상품과 전체 Key Account에서 아무도 사용하지 않은 상품은 추천하지 않는다.
- 추천 항목을 클릭하면 해당 상품을 쓰는 피어 고객사 칩을 펼치며, 칩을 클릭하면 해당 고객 상세로 이동한다.
- 화면 안내 문구(`.tabB.html`의 `.reco-legend`)는 3줄 구조다: ① `Key Account {고객수}개 고객이 사용하고 있는 상품 중에서 추천합니다.`(굵게, 고객수는 `customers.length`로 동적 바인딩) → ② `추천 우선순위 : ① 동종 업종 침투율순 → …` → ③ `추천 항목 클릭 시 사용 중인 피어 고객사 목록 표시`. 폰트 크기는 14px(`.reco-legend`, 전 화면 12px 강제 규칙보다 뒤에 선언해 우선 적용).

## 5. CSV 업로드 계약

- VDI의 AIP 문서보안 때문에 브라우저에서 XLSX가 열리지 않아, 업로드 포맷을 **CSV로 전환**했다. XLSX 파서(ZIP/XML, `DecompressionStream`)는 제거됐다.
- 브라우저에서 CSV 텍스트를 직접 파싱한다(`.surv.txt`의 `parseCsv`). 쉼표 구분, UTF-8, 선행 BOM 허용, RFC4180 큰따옴표 규칙(필드 내 쉼표·줄바꿈, `""` 이스케이프)을 처리한다. 첫 행이 헤더다.
- **업로드는 매출데이터 한 종류뿐이다**: `CSV 업로드` 버튼(`bUploadBtn`) 클릭 시 바로 파일 선택창(`bFile`)이 뜬다. 과거 있었던 "매출데이터/상품체계 선택 모달"과 상품체계 개별 업로드(`bFileTaxo`, `parseTaxo()`)는 제거됐다 — 상품체계는 더 이상 별도 파일로 관리하지 않는다(3절 참조).
- **매출데이터**(`bFile`, `.surv.txt`의 `parse()`) → 전역 `RAW`(메뉴판데이터, 고객×상품 매출)를 교체한다. 필수 컬럼: `고객ID`, `고객명`, `Tier`, `업종`, `손익코드`, `손익명`, `매출코드`, `매출명`, `25년매출`, `26년매출`, `대분류`, `중분류`, `AX구분_대`, `AX구분_중`. `AI Stack`은 필수가 아니다. `고객소통명`은 **선택 컬럼**이다 — 상품분류체계 팝업(`TAXO`)이 피벗 키로 쓰지만, 없는(구버전) CSV를 업로드해도 파싱은 정상 진행되며 이 경우 상품체계의 고객소통명 열은 빈칸으로 나온다.
- 매출데이터 업로드가 성공하면 그 즉시 `.surv.txt`의 `pivotTaxo(RAW)`로 전역 `TAXO`(상품체계데이터)를 재계산한다. 별도 업로드·저장 없이 항상 `RAW`에서 파생된다.
- 고객명은 표시용으로 정제한다(`PREAMBLE`의 `cleanName`): `(주)`, `㈜`, `주식회사`, `(유)`, `유한회사`, `(재)` 등 법인형 접두/접미를 제거한다. 원본의 다른 필드·계산에는 영향을 주지 않는다.
- 매출 필드는 천단위 콤마가 있어도(`"1,234"`처럼 따옴표로 감싼 경우) 숫자로 변환한다.
- **선행 0 보정**: 엑셀에서 xlsx → CSV로 저장할 때 셀 서식이 일반이면 `050`처럼 선행 0이 있는 값이 숫자로 변환돼 CSV에 이미 `50`으로 기록되는 경우가 있다(CSV 파일 자체에 정보가 유실된 상태라 파서 단에서 원본 복구 불가). 실 데이터에 존재하는 값에 한해 안전하게 매핑하는 safe rule을 `.surv.txt`의 공용 `LEADING_ZERO_FIX = {'50':'050','60':'060','80':'080','2':'002'}`로 뽑아 매출데이터의 매출명(`fixLeadingZero`)에 적용한다. 새로운 선행 0 값이 발견되면 이 맵에 추가한다. 근본 해결은 엑셀에서 해당 열을 텍스트 서식으로 재입력 후 CSV 재저장하는 것이다.
- **업로드 데이터 유지**: VDI에서는 `build.mjs`를 타지 않으므로(이미 빌드된 단일 HTML만 존재), 업로드한 데이터는 브라우저 JS만으로 유지한다.
  - 매출데이터는 업로드 성공 시 `localStorage`(키 `menuRawData`)에 자동 저장하고, 페이지 로드 시 자동 복원한다(별도 초기화 버튼 없음, 재업로드 시 해당 키만 자동 덮어씀). 저장 실패(quota 초과·시크릿 모드 등)는 조용히 무시하고 내장/빈 데이터로 폴백한다. `TAXO`는 저장 대상이 아니며, 복원된 `RAW`로부터 매번 `pivotTaxo()`로 재계산한다.
  - 단, `localStorage`는 그 브라우저·PC에만 남고 HTML **파일 자체**는 계속 빈 상태다. 다른 PC로 파일만 옮기면 데이터가 없다.
  - 파일 자체에 데이터를 박아 공유하려면 `파일로 저장` 버튼(`bSaveFileBtn`)을 쓴다 — 현재 문서의 `let RAW=[...]`와 `let TAXO=[...]` 둘 다 최신 데이터(`TAXO`는 `pivotTaxo(RAW)` 결과 스냅샷)로 치환한 새 HTML을 다운로드한다. 브라우저 정책상 원본과 파일명이 같으면 `(1)` 등 번호가 자동으로 붙으므로, 사용자가 다운로드된 파일을 원래 위치로 옮겨 기존 파일과 직접 교체해야 한다(자동 덮어쓰기는 브라우저 보안상 불가).

## 6. 검증과 주의사항

- 일반 검증은 1절의 4개 명령을 실행한다. `verify_tax.mjs`는 `/tmp/verify_tax_out.html`을 만들 수 있다.
- 시각 변경은 기본 `mockup.html`에서 확인한다. `mockup_variation/`의 파일은 이전 실험본이므로 검수 기준으로 사용하지 않는다.
- `verify_tax.mjs`의 오버레이 닫기 검증은 단일 합성 `click`만 보내므로 실제 `mousedown → click` 구현과 차이가 날 수 있다. 이 값만으로 회귀 판정하지 않는다.
- CSV 업로드는 배포용 `mockup-vdi.html`에서 확인한다. 필수 14컬럼 헤더 + 따옴표로 감싼 쉼표 포함 필드 + `(주)` 접두 고객명을 넣은 샘플로 파싱·정제·렌더를 점검한다.
- 기존 변경이 있는 작업 트리를 전제로 한다. 관련 없는 파일·실험본을 정리하거나 삭제하지 않는다.

## 7. 최근 변경

### 2026-07-22 — 현재 기준 UI 정리

- AI Stack 제거 및 AX 단일 분류화.
- 동종 고객/동종 업종 비교 화면을 분리하고, 두 비교표에 AX 대분류 표기를 추가.
- 동종 업종 비교는 전체 상품을 표시하고 업종공통/기준 고객의 보유 상태를 비교하도록 확정.
- 고객별 히트맵 헤더 높이 축소, 상세·비교 화면 여백 통일, 동종 고객 비교의 공통/차이 음영 정리.
- 전 화면 최소 글자 크기 12px 적용.

### 2026-07-22 — 마지막 반영 사항

- 상품별 커버리지 막대의 버건디 그라데이션을 고객별 히트맵과 동일한 농도 체계로 통일.
- 메인 안내 문구와 추천 로직 안내 문구를 최신 문구로 갱신.
- 동종 업종 비교의 업종공통 0명 `미보유`를 버건디 배지로 통일.

### 2026-07-23 — 인수인계 문서 정리

- 누적 작업 헤더와 시행착오 기록을 현재 상태 중심의 문서로 통합했다.
- 이후 변경은 이 섹션에 간결하게 추가하고, 안정화되면 해당 내용을 2~6절의 기준 문서에 반영한다.

### 2026-07-23 — 용어·정제·리사이저·CSV 전환

- **용어**: 단독 배지·셀 값의 `보유/미보유`를 `침투/미침투`로 통일(동종 고객 비교 셀, 동종 업종 비교 배지, 탭 A 비교표 셀). 서술형 문구(`공통 보유 상품`, `일부만 보유`, `보유한 업종 고객`)는 유지.
- **고객명 정제**: `cleanName`으로 법인형 접두/접미를 화면 표시용으로 제거(5절 참조).
- **탭 B 리사이저**: 고객별 현황 히트맵의 고객명 열을 드래그로 조절 가능(`--bc-cust-col-width`, 110~320px, 기본 230px). 데이터 컬럼은 `table-layout:fixed`로 남는 폭을 균등 분배한다. 리사이저 CSS/로직은 탭 A의 `col-resizer` 패턴을 재사용하되 셀렉터·변수만 탭 B용으로 분리했다.
- **CSV 전환**: XLSX 파서를 제거하고 CSV 업로드로 교체(5절 참조). 업로드 버튼·`accept`·안내 문구를 CSV로 갱신.

### 2026-07-23 — 탭 A(기존 메뉴판) 완전 제거, 단일 화면화

- 탭 바(작업중/기존 메뉴판 전환)와 탭 A를 모든 소스에서 제거했다. 화면은 이제 탭 없이 메뉴판 단독으로 바로 뜬다.
- `.secA.txt`(탭 A 마크업) 파일 삭제. `.surv.txt`에서 탭 A 전용 함수(`render`,`head`,`filtered`,`renderCompare`,`selectPeers`,`showDetail`,`show`,`block`,`panel`,`unused`,`bindCustomerResizer`)와 탭 A 이벤트 바인딩을 제거하고, 공용 함수(`agg`,`cats`,`majors`,`cap`,`tierClass`,`universe`,`peerStats`,`recommended`,`dims`,`parse`/`parseCsv`)만 남겼다. `dims`의 탭 A 필터 select 조작도 제거했다.
- `.tabB.js`의 `showTab`·탭 버튼 바인딩·탭 A 상태줄 동기화를 제거했다.
- `build.mjs` 조립부에서 탭 nav 마크업·`tabA-wrap`·`secA` 참조를 제거하고, 컨테이너를 `#tabB-wrap` 단독으로 남겼다.
- CSS: 확실히 미사용인 탭 A 전용 규칙만 제거(`.tabnav`/`.tabbtn`/`.tabpane` 블록, `.customer-head`/`.total-head`/`.table-wrap`). 12px 강제 오버라이드 셀렉터를 `.tabpane …` → `#tabB-wrap …`으로 변경해 규칙을 유지했다. 공용 규칙에 콤마로 섞인 `.tabbtn`(무해한 미사용 셀렉터)은 남겨두었다.
- 검증: `verify_ui.mjs`의 probe에서 탭 A 참조(`tabBtnB`/`tabBtnA`/`tabA-wrap`/`#tbody`)를 제거했다. 산출물 JS가 참조하는 모든 DOM id가 마크업에 존재함을 정적 확인(dangling 참조 0).
- **초기화 흐름 주의**: 예전에는 탭 A 초기화 IIFE가 최초 `dims()`를 호출해 `customers`/`industries`/`tiers`를 채웠다. 탭 A 제거 후에는 `.tabB.js` 말미의 초기화가 직접 `dims()`를 부른다(`dims();bFillFilters();renderHome();`). 이 `dims()` 호출이 없으면 `RAW`가 있어도 `customers`가 비어 화면이 렌더되지 않으므로, 초기화 순서를 건드릴 때 반드시 유지한다.

### 2026-07-23 — CSV 업로드 데이터 유지(localStorage 자동 복원 + 파일로 저장)

- VDI는 `build.mjs`를 타지 않고 이미 빌드된 단일 HTML만 쓰므로, 업로드 데이터 유지는 브라우저 JS로 해결했다(5절 참조).
- `.tabB.js`: 업로드 성공 시 `localStorage.setItem('menuRawData', ...)`로 자동 저장, 페이지 로드 시 자동 복원(`dims()` 이전에 `RAW`를 교체). 저장/복원 모두 `try/catch`로 감싸 실패 시 조용히 폴백.
- `.tabB.html`에 `파일로 저장`(`bSaveFileBtn`) 버튼 추가. 클릭 시 현재 문서의 `let RAW=[...];`를 최신 데이터로 치환한 새 HTML을 다운로드한다. `let RAW=\[[\s\S]*?\];` 정규식이 산출물에서 정확히 1회만 매칭됨을 확인했다(치환해도 다른 전역 변수·문법 영향 없음).
- 데모용 `mockup.html`과 배포용 `mockup-vdi.html` 둘 다 동일하게 적용(`.tabB.js`가 양쪽에 동일 삽입되므로 소스 1곳만 수정).
- CSV 업로드가 반영 완료되면 안내 모달(`bUploadDoneModal`)을 띄운다: "업로드 완료되었습니다. 우측 상단의 '파일로 저장' 버튼으로 다운받아 사용바랍니다." 확인 버튼 또는 오버레이 바깥 클릭 시 닫힌다. 기존 상품분류체계 모달(`bTaxModal`)의 `mousedown`+`click` 오버레이 판정 패턴을 재사용했다.

### 2026-07-24 — 실 업종 체계 반영, 그룹 카운트, 정렬/높이/추천 문구 개선

- **업종 그룹핑 버그 수정**: `INDUSTRY_ORDER`(`build.mjs`)에 없는 업종값은 `industryRank`가 전부 동률(99)로 반환돼, 실 CSV의 `대기업`/`글로벌` 등이 매출 순으로 흩어져 그룹이 여러 번 반복되던 문제를 발견. `INDUSTRY_ORDER`를 실 업종 6종 `['금융','대기업','온라인/IT','글로벌','공공','SME']`로 교체(3절 참조).
- 합성 데이터(`build.mjs`의 `INDUSTRIES`)도 동일 6개 업종·순서로 재구성(금융9·대기업9·온라인/IT9·글로벌9·공공9·SME8 = 53개사 유지). 업종별 상품 boost 로직도 새 업종명으로 매핑(제조→대기업, 유통→글로벌).
- 고객별 현황 그룹 헤더에 `업종명(고객수)` 카운트 표시 추가(`.tabB.js`의 `industryCounts`, 페이지네이션 무관 전체 기준).
- 서비스 수/매출 기준 토글 시 정렬 2차 키를 분리: `service`=`usedTotal`, `sales`=`salesTotal` 내림차순(5절 참조).
- CSV 업로드 시 상품명(매출명) 선행 0 보정 safe rule 추가: `50→050`,`80→080`,`2→002`(5절 참조).
- 고객별 현황 테이블(`.bc-wrap`) 세로 높이를 고정 640px → `calc(100vh - 260px)`로 확장.
- 추천 로직 안내 문구를 3줄 구조로 개편하고 `Key Account {동적 고객수}개 고객` 원칙을 강조, 폰트 14px로 확대(4절 참조).
- `TEAM_HANDOFF.html`(팀 인수인계 슬라이드형 문서)도 함께 정리: 단계 순서 조정, 쉐어포인트 폴더 경로·파일명 상세화, 이미지 2장을 base64 data URI로 인라인화해 파일 단독 공유 가능하게 함. 이 파일은 `build.mjs` 산출물이 아니라 별도 정적 문서이므로 `node build.mjs`와 무관하다.

### 2026-07-24 — 업종공통 침투율(%) 표시, TEAM_HANDOFF 피드백 섹션

- 동종 업종 비교의 업종공통 `m/n` 표시 뒤에 `(백분율%)`를 추가(예: `6/9 (67%)`). 0명(미침투)일 때는 표시하지 않는다(3절 참조). `.tabB.js` 수정 후 `mockup.html`/`mockup-vdi.html`에 동일 반영.
- 업종공통 칸은 값 길이(`6/9`~`6/9 (100%)`)가 들쭉날쭉해 기존 중앙정렬(`text-align:center`)로는 시작 위치가 흔들렸다. 업종공통 span에만 `industry-stats-common` 클래스를 추가해 좌측 정렬(`.tabx.css`)로 고정하고, 헤더 `업종공통` 라벨도 동일하게 좌측 정렬해 열을 맞췄다. 기준 고객 칸(침투/미침투 배지)은 기존 중앙정렬 그대로다.
- `(백분율%)`는 `n/m`(13px, `<b>`)보다 1px 작은 12px로 별도 `<small>` 처리(`.industry-stats small`, 최소 글자 크기 12px 규칙과 정확히 일치).
- `TEAM_HANDOFF.html`(git 추적 대상 아님, `.gitignore`)에 메인 타이틀과 `전체 흐름` 사이 `피드백 사항` 섹션 추가: ① 배포(Delivery) 방법 검토 필요(CEO가 주로 폰/패드로 확인), ② 로컬에서만 챗봇 동작하는 문제(팀즈 링크로 열 때 챗봇 동작 필요).
- 위 ②번 카드의 스크린샷(VDI 네트워크/CORS 차단 오류)은 `<details class="err-toggle"><summary>오류메세지 확인</summary>...</details>`로 감싸 기본은 접힌 상태, 클릭 시 거의 원본 크기(`max-width:720px`)로 펼쳐지게 했다. JS 없이 네이티브 `<details>`로 구현.

### 2026-07-31 — 상품체계(TAXO) 데이터 분리, 상품분류체계 팝업 개편, 업로드 종류 선택

- **배경**: 상품분류체계 팝업이 그동안 매출데이터(`RAW`)에서 실제 쓰인 조합만 역산해 보여주는 방식이었다. 사내 상품체계 참조문서(`ent-서비스분류체계-메뉴판적용-v1.0.xlsx`, 컬럼: `AX구분(대)/AX구분(중)/대분류/소분류/손익명/고객소통명`)를 반영해, 매출 여부와 무관하게 전체 상품체계를 보여주는 별도 데이터셋으로 분리했다. xlsx 실 데이터는 그대로 쓰지 않고 구조만 따라 합성 데이터로 채웠다(요청사항).
- **`TAXO` 전역 추가**: `RAW`(매출데이터)와 별개인 상품체계데이터. `bTaxonomyRows()`가 `RAW` 대신 `TAXO`에서 렌더링(`.tabB.js`). 팝업 표가 4열→6열(`소분류`·`고객소통명` 추가) + 맨 우측 `매출발생` 체크박스 1열로 확장(`.tabB.html`, `table.tax` 폭 60%→100%).
- **업로드 버튼 이원화**: `CSV 업로드` 클릭 시 `매출데이터`/`상품체계` 선택 모달(`bUploadChooseModal`)이 뜨고, 각각 `bFile`(기존 `parse()`)/`bFileTaxo`(신규 `parseTaxo()`, `.surv.txt`)로 갈라진다. `localStorage` 키도 `menuRawData`/`menuTaxoData`로 분리, `파일로 저장`은 `RAW`·`TAXO` 둘 다 치환(5절 참조).
- **손익명 유사도 매칭**: `RAW`와 `TAXO`는 독립적으로 관리되는 데이터라 손익명 표기가 완전히 같지 않을 수 있다는 요청사항을 반영해, `.surv.txt`에 `taxSimilar()`(정규화 후 완전일치/포함관계/바이그램 Dice≥0.6)를 추가하고 이 결과로 `매출발생` 체크 여부를 판정한다(5절 참조).
- **정적참조 원칙**: `TAXO`는 고객 매출과 달리 비밀정보가 아니므로 데모(`mockup.html`)·배포(`mockup-vdi.html`) 빌드 모두에 동일한 합성 `TAXO_DEMO`(`build.mjs`, `CATALOG` 27종 + 화이트스페이스 데모용 신규 상품 3종)를 기본 내장한다. `RAW`만 배포용에서 비운다.
- `verify_ui.mjs`(업로드 버튼→선택모달→파일선택 2단계 흐름 검증)와 `verify_tax.mjs`(7열·체크박스 개수 검증) 갱신.
- **미반영 항목**: `mockup_v_m.html`/`mockup-vdi_m.html`(모바일 변형, `build.mjs` 산출물 아님)은 이번 변경에 포함하지 않았다 — 사용자 확인 후 별도 반영 필요.

### 2026-07-31 — 서브탭 순서 변경, 고객별 현황 기본 펼침

- `bNav` 서브탭 순서를 `상품별 현황/고객별 현황` → `고객별 현황/상품별 현황`으로 변경(`.tabB.html`). 활성 탭(기본 진입 화면)은 기존과 동일하게 고객별 현황이다.
- 고객별 현황 리스트의 기본 상태를 `접기(페이지네이션)` → `펼치기(전체 표시)`로 변경. `bcExpanded` 초기값을 `false`→`true`로, 매출데이터 업로드 시 리셋값도 동일하게 `true`로 맞췄다(`.tabB.js`). 진입 시 `전체 펼치기` 대신 `접기` 버튼이 보인다.
- `verify_ui.mjs`의 고객별 현황 검증 순서를 "기본 펼침 상태 확인 → 접기 → 페이지네이션/필터 확인 → 재펼침" 순으로 재구성해 새 기본값을 반영.

### 2026-08-03 — 상품체계를 매출데이터 피벗으로 전환, CSV 업로드 단일화

- **배경**: 상품체계(`TAXO`)를 별도 CSV로 관리하다 보니 매출데이터(`RAW`)와 표기가 어긋나는 문제(손익명 유사도 매칭으로 대응)가 있었다. 상품체계를 아예 매출데이터에서 직접 피벗해 만들도록 요청받아, 상품체계 개별 업로드 자체를 없앴다.
- **업로드 단일화**: `CSV 업로드` 버튼 클릭 시 뜨던 "매출데이터/상품체계" 선택 모달(`bUploadChooseModal`)을 제거하고, 클릭 즉시 매출데이터 파일 선택창(`bFile`)이 열리도록 변경(`.tabB.html`, `.tabB.js`). 상품체계 개별 업로드(`bFileTaxo`, `bUploadSalesBtn`, `bUploadTaxoBtn`, `parseTaxo()`, `REQ_TAXO`, `localStorage` 키 `menuTaxoData`)와 손익명 유사도 매칭(`taxSimilar()`, `normTax()`)을 전부 제거했다.
- **`pivotTaxo()` 신설**(`.surv.txt`): 매출데이터(`RAW`)를 `AX구분_대/AX구분_중/중분류/매출명/고객소통명` 5개 키로 유니크 그룹핑해 `TAXO`를 생성한다. 매출데이터 업로드 성공 직후와 `localStorage` 복원 직후 항상 `TAXO=pivotTaxo(RAW)`로 재계산하며, `TAXO` 자체는 저장/복원 대상이 아니다.
- **고객소통명 필드 추가**: 매출데이터 CSV에 `고객소통명` 컬럼을 추가해 파싱한다(`parse()`, `.surv.txt`). **선택 컬럼**이다 — 없는(구버전) CSV도 그대로 업로드 가능하며, 이 경우 피벗된 상품체계의 고객소통명 열은 빈칸이 된다. 샘플 파일 `샘플데이터.csv`(프로젝트 루트)에 이미 반영돼 있다.
- **상품분류체계 팝업 컬럼 축소**: 6열(`AX구분(대)/AX구분(중)/대분류/소분류/손익명/고객소통명`) + `KA여부` 체크박스 → 5열(`AX구분(대)/AX구분(중)/중분류/매출명/고객소통명`)로 축소(`bRenderTaxonomy()`, `.tabB.js`). 피벗된 모든 행이 정의상 매출이 존재하므로 `KA여부`(매출발생 체크) 컬럼과 관련 CSS(`.tax-mark`, `.tax-used`)를 제거했다. 헤더의 `(N종)` 고유값 개수 표기도 제거해 각 헤더가 단순 라벨만 표시한다.
- **`build.mjs` 정리**: 합성 상품체계 데이터 `TAXO_DEMO`(및 화이트스페이스 데모용 신규 상품 3종)를 제거했다. 이제 `TAXO`는 정적으로 굽지 않고 `let TAXO=[];`로만 초기화하며, 브라우저에서 `RAW`가 준비되는 즉시 `pivotTaxo(RAW)`로 채워진다. 데모용 합성 고객 데이터(`mkRow`)에 `custComm` 필드를 추가했다(매출명과 동일 값 부여).
- `verify_ui.mjs`(업로드 클릭 시 파일선택 직결 검증으로 교체, TAXO 개별 업로드 관련 검증 삭제)와 `verify_tax.mjs`(KA여부 체크 카운트 검증 삭제) 갱신.
- 개수 표기 제거에 따라 미사용이 된 `.tax-count`(`.tabx.css`) CSS 규칙도 함께 삭제.
- 상품분류체계 팝업의 데이터 셀(`table.tax td`)을 가운데 정렬로 변경(`.tabx.css`). 헤더(`table.tax th`)는 기존부터 가운데 정렬이었다.
- **AX구분(중)별 교대 줄무늬(zebra) 배경**: `bRenderTaxonomy()`가 `AX구분(대)+AX구분(중)` 조합이 바뀔 때마다 `tax-stripe` 클래스를 토글해 부여한다(`.tabB.js`). `TAXO`(`pivotTaxo()` 결과)는 `axMajor→axMiddle→...` 순으로 이미 정렬돼 있어 같은 조합은 항상 연속 구간을 이루므로, 행을 순회하며 직전 조합과 비교하는 것만으로 구간 경계를 판별할 수 있다. 판별 키를 `axMiddle` 단독이 아니라 `axMajor+axMiddle` 조합으로 잡은 이유: `AX/Infra` 구간 바로 뒤에 `Legacy/Infra` 구간이 오는 경우처럼 대분류가 바뀌어도 중분류값이 같으면 줄무늬가 이어지는 문제가 있어, 두 값을 합쳐 키로 써서 실제 그룹 경계와 항상 일치하게 했다. CSS(`.tabx.css`): `tr.tax-stripe td`는 `var(--u-surface-sub)`, `tr.tax-stripe td.tax-key`는 `var(--u-primary-tint-strong)`. 기존 `tr:hover td` 배경이 줄무늬와 같은 색이라 hover 시 구분이 안 되는 문제가 있어, hover 배경을 `var(--u-primary-tint)`(`!important`로 줄무늬보다 우선)로 변경했다.
- **AX구분(대)/AX구분(중) 셀 병합(rowspan)**: 같은 값이 반복되는 구간마다 매 행 반복 출력하던 것을, 구간의 첫 행에만 `rowspan=구간길이`를 부여하고 나머지 행은 해당 `<td>` 자체를 생략하도록 변경(`bRenderTaxonomy()`, `.tabB.js`). `rows`가 이미 `axMajor→axMiddle→...` 순 정렬돼 있어 동일 값은 항상 연속 구간이라는 성질을 이용, 각 행 인덱스마다 `axMajor`/`axMajor+axMiddle` 조합이 직전 행과 같으면 `span=0`(셀 생략), 다르면 그 지점부터 같은 값이 몇 행 이어지는지 미리 앞을 보고 세어 `span=N`으로 계산한다(`majorSpan`/`middleSpan` 배열, `.tabB.js`). 합성 데모 데이터(27행)로 `majorSpan`/`middleSpan` 합이 정확히 27이 되는 것을 확인해 셀 누락·중복이 없음을 검증했다. 병합된 셀은 여러 행 높이를 차지하므로 텍스트가 세로 중앙에 오도록 `table.tax td{vertical-align:middle}`을 추가했다(`.tabx.css`).
- **AX구분(대) 값별 배경색 + hover 강조**: 실 데이터상 `AX구분_대`는 `AX`/`Legacy` 2종류뿐이라, 2톤으로 구분했다(`bRenderTaxonomy()`, `.tabB.js`가 값에 따라 `tax-major-ax`/`tax-major-legacy` 클래스를 부여, `AX`가 아닌 값은 모두 `legacy`로 폴백). CSS(`.tabx.css`): `tax-major-legacy`는 `var(--u-surface-head)`(중립 톤). 줄무늬(`tax-stripe`) 상태에서도 값별 톤이 유지되도록 별도 규칙을 추가했다.
  - **AX 톤 변천: 버건디 → 블루 → Legacy와 동일 톤 → 회색조 내에서만 미세 구분**: 처음엔 `tax-major-ax`를 브랜드색 `var(--u-primary-tint-strong)`(버건디)로 잡았다가, 상품분류체계 팝업 전반의 hover 등 다른 곳에서도 버건디 계열(`--u-primary-tint`)을 이미 쓰고 있어 AX 행만 시각적으로 구분되지 않는다는 피드백을 받아 옅은 블루 계열(`rgba(37,99,235,α)`, `#2563eb`)로 바꿨다. 이후 AX와 Legacy를 같은 톤으로 통일해달라는 요청, 곧이어 "AX/Legacy가 구분되긴 해야 하니 한 단계만 톤다운"해달라는 요청으로 블루를 유지한 채 알파값을 낮췄으나, 최종적으로 "회색톤으로만 구분"해달라는 요청을 받아 색상(hue) 자체를 없앴다. Legacy는 중립 회색(`var(--u-surface-head)`/`var(--u-border)`/hover `rgba(28,27,27,.035)`) 그대로 두고, AX는 파란기 없는 순수 검정 알파로만 그보다 살짝 진하게 잡았다: 기본 `rgba(28,27,27,.045)`, 줄무늬 `rgba(28,27,27,.075)`, hover `rgba(28,27,27,.055)`(Legacy hover `.035`보다 살짝 진함). 글자색(`color`)은 AX/Legacy 모두 `var(--u-onvariant)`로 통일해 배경 톤 차이만으로 구분되게 했다. `tax-major-ax`/`tax-row-ax` 클래스와 `AX`/`Legacy` 값 분기 로직(`bRenderTaxonomy()`)은 계속 유지된다.
  - **hover 시 Legacy 행도 핑크로 보이던 버그 수정**: 처음엔 hover 색 분기를 `tax-key`/`axMiddle` 셀(rowspan된 셀)에만 걸었는데, 실제 `tr:hover td{background:var(--u-primary-tint)!important}` 공통 규칙은 **행의 모든 `<td>`**(중분류/매출명/고객소통명 등 rowspan 안 된 일반 셀 포함)에 적용된다. rowspan 셀에만 값별 hover 규칙이 있고 일반 셀에는 없다 보니, Legacy 행에 마우스를 올려도 일반 셀들은 전부 공통 핑크색(`--u-primary-tint`)으로 칠해져 대분류와 무관하게 hover 색이 핑크로 보였다. `<td>`가 아니라 **`<tr>` 자체**에 `tax-row-ax`/`tax-row-legacy` 클래스를 부여하고, hover 규칙도 `tr.tax-row-ax:hover td`/`tr.tax-row-legacy:hover td`(행의 모든 셀 대상)로 바꿔 해결했다.
  - **hover 색 옅게 재조정(2단계)**: 처음 적용한 hover 색(`tax-row-ax`: `rgba(185,0,100,.14)`, `tax-row-legacy`: `var(--u-border-strong)`)이 기본 배경 대비 너무 진하다는 피드백을 받아 1차로 `tax-row-ax`를 `rgba(185,0,100,.11)`, `tax-row-legacy`를 `var(--u-border)`로 낮췄으나, 여전히 진하다는 재피드백을 받아 2차로 더 낮췄다. `tax-row-ax`는 당시 `rgba(185,0,100,.095)`(기본 배경 `--u-primary-tint-strong`의 `.08`에 더 근접, 새 토큰이 없어 인라인 지정). `tax-row-legacy`는 고정 회색 토큰(`--u-border`)이 배경과 무관하게 항상 같은 진하기라 옅게 느껴지지 않는 문제가 있어, 토큰 대신 기본 배경(`--u-surface-head`, `#f5f0e8`) 위에 아주 옅은 검정 알파를 얹는 `rgba(28,27,27,.035)`로 교체했다. (이후 AX 계열 색상 자체가 블루로 바뀌면서 `tax-row-ax` hover도 `rgba(37,99,235,.095)`로 같은 알파값을 유지한 채 색상만 교체됐다 — 위 항목 참조.)

### 2026-07-31 — 상품분류체계 헤더에 고유값 개수 표시, 매출발생→KA여부 개명

> 이하 항목의 `KA여부` 컬럼·손익명 유사도 매칭은 위 2026-08-03 변경으로 제거됐다. 과거 맥락 기록으로 남겨둔다.

- 상품분류체계 팝업 표 헤더(`대분류`/`소분류`/`손익명`/`고객소통명`)에 고객별 현황 히트맵 헤더(`bHeadHtml()`의 `<small>N종</small>`)와 동일한 패턴으로 고유값 개수를 표시(`대분류<br><small>13종</small>` 형태). `매출발생` 컬럼은 `KA여부`로 이름을 바꾸고 체크된(사용) 건수를 함께 표시(`KA여부<br><small>27건</small>`).
- `<thead>`를 정적 마크업(`.tabB.html`)에서 동적 렌더링(`bRenderTaxonomy()`, `.tabB.js`)으로 옮겼다 — 개수가 `TAXO`/매칭 결과에 따라 바뀌므로 매번 계산해야 함.
- 헤더 하단 개수 표기는 `(13종)`처럼 괄호로 감싸고, 라벨을 포함한 `table.tax th` 전체를 가운데정렬로 바꿨다(데이터 행 `td`는 기존대로 왼쪽정렬 유지) — 처음엔 개수(`.tax-count`)만 따로 중앙정렬했으나 라벨과 개수 줄이 서로 어긋나 보여, 헤더 셀 전체를 중앙정렬로 통일했다.
- 개수 표기는 `손익명`/`고객소통명`/`KA여부` 3개 헤더에만 남기고 `대분류`/`소분류`에서는 제거했다(`bRenderTaxonomy()`, `.tabB.js`).
- **KA여부 체크 표시**: `disabled` 네이티브 체크박스는 브라우저가 강제로 흐리게(회색) 렌더링해 CSS(`accent-color`/`opacity`)로도 진하게 바꿀 수 없었다(Chromium에서 실측 확인). 대신 커스텀 마크(`.tax-mark`, 버건디 배경+흰 체크 `on` / 테두리만 `off`)로 교체해 브랜드 색상과 통일하고 가독성을 높였다. `verify_tax.mjs`의 체크/미체크 카운트 셀렉터도 `input[type=checkbox]`→`.tax-mark.on`/`.tax-mark.off`로 갱신.

## 8. 진행중 시안 — mockup_v2/mockup_v2_option-c.html (2026-08-07 ~)

> **이 절은 `mockup.html`과 무관한 별개 파일 작업이다.** `mockup_v2/`는 1절의 "생성 산출물"이 아니며 `build.mjs`가 건드리지 않는다. 3개 시안(A/B/C) 중 옵션 C(단일 캔버스 인라인 드릴다운)를 골라 계속 발전시키는 중이며, `mockup.html`에 반영할지는 사용자 확인 후 별도 결정한다. 이 절이 아래 항목들로 안정화되면 해당 시점에 1~7절 편입 여부를 재검토한다.

> **이 문단은 초기 진행 기록이다.** 2026-08-07 전환 이후의 현재 메인 소스·빌드·기능 범위는 아래 `mockup_v2.html` 메인 소스 전환 절을 기준으로 본다.

### 배경

- `4d5ede0` 커밋(현재 HEAD 히스토리 밖의 커밋)에서 "KA전체뷰→업종별 소계→고객별 뷰" 3단계 드릴다운 시안 3개(표형=A, 카드그리드형=B, 인라인아코디언형=C)가 조립 스크립트(`_build.mjs` 등)와 함께 추가됐었다. 현재 워킹트리에는 조립 스크립트 없이 완성된 단일 HTML 3개만 남아 있다(`mockup_v2/`는 git상 untracked). 사용자가 옵션 C를 골라 이 파일 하나만 직접 수정하며 디벨롭 중이다.
- 수정 전 백업: `legacy/.mockup_v2_option-c.html.bak`.

### 현재 화면 구조 (단일 화면 + 탭 3개)

- KPI 스트립(고정) → 탭(`KA 전체`/`업종별`/`고객별`) → 탭별 패널. 별도 화면 전환 없이 한 페이지 안에서 전환된다.
- **KPI 스트립**: `KA 고객 수` → `KA 총매출`(버건디 강조) → `당사 상품 중 KA 침투상품`(버건디 강조) → `고객당 평균 사용 상품`. 진행률 바(`.sp`/`cvKpiBar`/`cPenBar`)는 제거했다 — 카드가 그만큼 얇아짐. `KA 총매출` 카드는 다른 카드와 동일한 흰 배경(어두운 `.hl` 배경 제거).
- **KA 전체 탭**: `mockup.html`의 '상품별 현황'(`renderOverviewPane`/`renderNode`) 구조를 그대로 이식한 커버리지 트리. `axTopRows`/`axMidRows`/`svcRowsInAxKey`/`svcRowsFlat`을 옮겨왔다(`covRow()`, `covSortRows()`, `covNodeKey()`, `covChildrenOf()`). 카테고리(대분류→중분류→세부상품 드릴다운)/세부상품(27개 통합 평탄 리스트) 토글, 커버리지 높은순/낮은순 정렬 토글 제공.
  - 모든 레벨의 축은 "53개사 중 사용 고객사 수"로 통일(`mockup.html`과 동일). 대분류·중분류는 90%대로 뭉치지만 이는 문제가 아니라 정렬·화이트스페이스 표시로 예외를 부각시키는 게 원본 설계다.
  - 기본 화면에서 대분류·중분류까지는 펼쳐진 상태(`openToMidLevel()`)이고 세부상품 리프만 접혀 있다.
  - 세부상품 리프를 클릭하면 침투/미침투 고객을 업종별로 묶어 펼친다(`penetrationGroup()`). 각 고객은 칩 버튼이며 클릭 시 고객 상세로 즉시 이동한다(`showDetail()`).
  - 화이트스페이스(커버리지 40% 미만)는 행 배경 음영(`.cov-row.whitespace`)으로만 표시하고 배지는 없앴다가, 사용자가 "범례 설명에는 배지를 다시 달라"고 해 범례(`ws-legend`)에만 `[화이트스페이스]` 텍스트 배지를 복원했다. 행 자체에는 배지 없음.
  - **커버리지 막대 표현 변경**: 처음엔 막대 안에 흰 글씨로 `%`를 넣었는데, 커버리지가 낮으면(15% 등) 채워지지 않은 밝은 배경 위에 흰 글씨가 걸쳐 안 보이는 문제가 있었다(`mockup.html`에도 있던 잠재 버그). 1차로 "막대 폭 20% 미만이면 텍스트를 막대 바깥 오른쪽에 어두운 글씨로" 조건부 처리했으나, 값에 따라 텍스트 위치·색이 바뀌는 게 스캔하기 나쁘다는 피드백을 받아 최종적으로 **막대를 순수 시각 게이지로만 두고 `%` 텍스트는 완전히 제거, 대신 우측 `.cov-count` 칼럼에 항상 고정 위치로 크게 표시**(`<b>15%</b><small>8/53개사</small>`)하는 방식으로 확정했다. 막대 높이도 20→14px로 줄였다.
- **업종별 탭**: 업종 6개 → 클릭 시 소속 고객사 아코디언. 각 행은 "5분할" 히트맵(`전체 + AX Infra + AX Application + Legacy Infra + Legacy Application`, `g5Row()`/`g5Cell()`), mockup.html과 동일한 히트맵 칩(배경 농도 `0.28+ratio*1.1`, 최대 .98) 표현이다.
  - 업종 소계 막대는 "업종이 쓰는 상품 개수"가 아니라 5칸 각각 자기 중분류 카탈로그를 모수로 쓴다 — 그래야 업종끼리 85~96%로 뭉치지 않고 셀 단위로 변별력이 생긴다.
  - 헤더(`g5Head()`)에 AX/Legacy 대분류 라벨을 얹어 계층을 드러낸다. `[전체] | [AX Infra·App] | [Legacy Infra·App]` 구획을 세로선으로 구분하고(헤더 가로 밑줄은 제거), 칩끼리는 `width:calc(100% - 6px);margin:0 3px`로 살짝 좁혀 여백을 냈다.
  - 들여쓰기는 `.g5`(행 전체)가 아니라 `.g5-nm`(이름 칸) 안쪽 패딩으로만 준다 — 행 전체에 패딩을 주면 그리드 첫 칼럼 폭이 줄어 업종 행과 고객 행의 히트맵 칩이 서로 다른 위치에서 시작하는 버그가 있었다.
- **고객별 탭**: 53개사 전수를 같은 5분할 히트맵으로 단일 정렬 표시. 별도 화면이 아니라 탭 안에 있다.
  - **고객 상세 화면**(`vDetail`, `showDetail()`): `mockup.html`의 `bDetail`/`recommended()`를 상품 단위 집계(`aggCust()`)와 함께 이식했다. AX 대분류별 사용 중 상품 + 추천 상품(4단계 우선순위: ①동종업종 침투율→②동종업종 평균매출→③타업종 침투율→④타업종 평균매출, 최대 3개), 추천 클릭 시 피어 고객사 칩 펼침. (초기 기록상 비교 화면은 제외했으나, 현재 메인 소스에는 동종 업종·동종 고객 비교가 추가됐다.)
- **Tier 표시 제거**: 화면 어디에도 Tier 배지를 노출하지 않는다. 데이터의 `tier` 필드 자체는 매출 규모 산정(`SIZE={Tier1:3.0,...}`)에 계속 쓰이므로 삭제하지 않았다.

### 폰트·반응형

- 전체 화면 최소 폰트를 12px 기준으로 재조정했다(기존 최소 11px). 11→12, 12→13, 13→14, 14→15, 15→16, body 16→17px, 22·23px는 24px로 통합.
- 970px~761px 구간에서 `.cov-row`(5칼럼: `220px 1fr 100px 116px 32px`)와 `.g5`(6칼럼: `210px repeat(5,1fr)`) 둘 다 대응하는 중간 브레이크포인트가 없어 고정폭 칼럼이 화면폭을 거의 다 먹어 시인성이 나빴다. `@media(max-width:970px)`를 양쪽에 추가해 칼럼 폭·폰트를 완화했다.

### 알려진 이슈 / 다음 확인 사항

- 매출 구성 스택바 추가(원래 요청 3번)는 사용자가 "일단 보류"로 명시 — 아직 미착수.
- `mockup_v2/`가 git untracked 상태라 일반 `git status`/`git diff`로 이력 추적이 안 된다. 필요 시 `git add`로 편입할지 사용자와 확인 필요.
- 이 시안이 확정되면 `mockup.html` 반영 여부와 반영 범위(3~6절 편입, `build.mjs` 소스 조각화 등)를 사용자와 별도로 정해야 한다.

## 9. 경영진용 통합 시안 — mockup_v3/ka_executive_dashboard.html (2026-08-07)

> 기존 `mockup.html`과 `mockup_v2/`는 변경하지 않고, 실제 더미 CSV를 반영한 별도 시안으로 분리했다.

### 파일과 빌드

- `mockup_v3/dashboard.source.html`: UI·집계·상호작용 소스. CSV 문자열 자리표시자를 포함한다.
- `mockup_v3/build.mjs`: `더미데이터_매출.csv`와 `더미데이터_월렛쉐어.csv`를 소스에 내장해 단일 HTML을 만든다.
- `mockup_v3/ka_executive_dashboard.html`: 브라우저에서 서버 없이 실행하는 최종 산출물.
- `mockup_v3/verify.mjs`: HTML ID 중복, 스크립트 구문, 53개 고객·63개 상품·월렛 유효 52개 고객, 2025/2026 매출 합계를 검증한다.

```bash
node mockup_v3/build.mjs
node mockup_v3/verify.mjs
```

### 정보 구조와 화면

- 상단 탐색은 `KA 전체 → 업종별 → 고객별` 3단계다.
- `KA 전체` 첫 화면 순서는 `한 문장 결론 → 핵심 KPI 4개 → 경영 포커스 3개 → 우선순위 큐 → 업종별 요약`이다. 상품 검색·필터와 고객 포지션 맵은 하단 `상품·고객 상세 보기` 접힘 영역으로 내렸다.
- 최상단 KPI에는 `KA 사용 상품 63종`을 표시하고, 보조 문구로 `전체 상품 132종 중`임을 설명한다. KA 상품 침투율·고객 사용률의 분모는 63종이다.
- `업종별`: 6개 업종 비교, AX/Legacy 4개 구분의 고객 평균 상품 침투 히트맵, 선택 업종의 고객 실행 목록, 업종 내 확산 후보.
- `고객별`: 고객명·업종·실행 구간 필터와 정렬, 고객 상세의 월렛 구성, 동종 업종 순위, 추천 상품, 사용 상품 목록.
- 매출 기간은 `2026 YTD`와 `2025 연간`을 전환하되 기간 길이가 달라 YoY는 계산하지 않는다.
- 월렛쉐어 데이터에는 기간 열이 없으므로 기간 토글과 무관한 고정 지표로 표시한다.

### 데이터·집계 규칙

- 매출 794행, 고객 53개사, 월렛 120행을 내장한다.
- 전체 상품 132종은 사용자가 제공한 전사 상품 수를 KPI 표시용으로만 사용한다. CSV에 등장한 KA 상품 63종만 KA 침투율 계산에 사용한다.
- 상품 모수는 매출 CSV에 실제 등장한 `매출코드+매출명` 조합 63종이다. `M31` 코드가 상품명 2종에 연결돼 코드 단독 집계 시 합쳐지므로 반드시 조합 키를 유지한다.
- 상품 사용 여부는 선택 기간의 고객×상품 매출 합계가 0보다 큰 경우다. 음수 조정 매출은 총매출에는 포함하되 사용으로 세지 않는다.
- 월렛쉐어는 고객명으로 매출 데이터와 연결하고 `WLSR_YN=Y` 행만 합산한다. 유효 고객은 52개사이며 `고객 050`은 미산정으로 표시한다.
- 월렛쉐어는 전체 고객의 월렛을 합산해 `ΣUPLS ÷ Σ(UPLS+KT+SK+ETC)`로 산정한다. 화면 표기는 `월렛쉐어`로 통일하고, 업종·고객 화면도 같은 원칙을 쓴다.
- 실행 구간은 전체 고객의 상품 침투율 중앙값과 월렛쉐어 중앙값을 교차한 상대 분류다: `크로스셀 우선`, `전환 우선`, `진단 우선`, `방어·확대`. 월렛 미산정 고객은 별도 분리한다.
- 추천 상품은 해당 고객의 미사용 상품 중 선택 기간에 다른 고객이 사용하는 상품만 대상으로, 동종 업종 침투율 → 동종 평균매출 → 타 업종 침투율 → 타 업종 평균매출 순으로 정렬한다. 데이터에 없는 예상 매출은 계산하지 않는다.
- 화면의 40% 미만 상품 수는 제공 CSV에 등장한 63종을 모수로 한 KA 침투율이며, CSV에 아예 없는 전사 상품은 포함하지 않는다.

### 데이터 교체

- 우측 상단 `데이터 교체`에서 매출/월렛 CSV 중 변경할 파일만 선택할 수 있다.
- 적용 데이터는 브라우저 `localStorage`에만 저장되며 외부로 전송하지 않는다. `기본 더미데이터 복원`으로 내장 CSV로 되돌릴 수 있다.

## 10. v2 Option C 실제 더미데이터 적용 (2026-08-07)

- `legacy/mockup_v2_option-c.template.html`: 초기 Option C 화면 원본 템플릿.
- `mockup_v2/build_option_c.mjs`: 매출·월렛쉐어 CSV를 템플릿에 내장하는 빌드 스크립트.
- `mockup_v2/mockup_v2_option-c.html`: 실제 더미데이터가 포함된 브라우저 실행용 결과물.
- `mockup_v2/verify_option_c.mjs`: 고객 수·상품 모수·월렛쉐어·기간 전환·고객 상세를 검증한다.

```bash
node mockup_v2/build_option_c.mjs
node mockup_v2/verify_option_c.mjs
```

- 매출 CSV의 53개 고객과 2025/2026 매출을 화면에 연결했다. 상품 사용 여부는 선택 기간 매출이 0보다 큰 경우로 계산한다.
- `M31`의 상품명 변형 2개는 `M2M차량`으로 통합했다. 따라서 상세 상품 목록에는 CSV 기준 관측 상품 62종이 표시되며, 상단 KPI와 전체 상품 사용률 분모는 사업부 제공 KA 상품 모수 63종을 유지한다.
- 월렛쉐어는 월렛 CSV의 `WLSR_YN=Y` 행을 고객명으로 매출 고객과 연결해 표시한다. 53개 고객 중 52개가 유효하고, `고객 050`은 미산정으로 표시한다. 전체 월렛쉐어는 `ΣUPLS ÷ Σ(UPLS+KT+SK+ETC)`로 계산한다.
- 상품별 현황에는 `매출`, `평균매출`, `커버리지`를 명시하고, 평균매출은 사용 고객 기준으로 계산한다. 매출이 정확히 0이면 `-`, 0.5억 미만의 양수 금액은 소수 억 단위, 0.01억 미만은 원 단위로 표시한다.
- 상품 커버리지 정렬 버튼은 제거하고 기본 높은 커버리지순으로 고정했다. 행의 사용 기준은 선택 기간 양수 매출이며, 0/53 항목에는 해당 기준을 툴팁으로 표시한다.

### 2026-08-07 — v2 Option C 업종·고객 매트릭스 정리

- `KA 전체` 화면은 상시 `매출·평균매출·사용상품비중`을 함께 표시한다. `사용상품비중/매출` 토글은 `업종별`·`고객별` 매트릭스 영역에만 노출되도록 이동했다.
- 매트릭스의 `전체` 열은 토글과 무관하게 매출과 `사용상품 / 전체상품`을 항상 함께 표시한다. 토글은 AX/Legacy 네 개 구분 칸에만 적용된다.
- 매트릭스 모드 라벨을 `사용상품비중`과 `매출`로 정리하고, 축 설명은 서비스 모드에서 `사용상품 / 전체상품`으로 표시한다.
- 전체 영역의 `월렛쉐어`, `매출`, `사용상품 / 전체상품`을 각각 독립 컬럼으로 분리했다.
- 전체·AX·Legacy 구획 사이에 별도 1px 칸을 둬 헤더와 행을 관통하는 동일한 세로 구분선을 적용하고, AX/Legacy 그룹명은 두 하위 칸의 중앙에 배치했다. 그룹명 아래의 붉은 가로선은 제거했다.
- 매트릭스 칩 사이의 좌우·상하 간격을 넓혀 업종/고객 행의 수치를 분리해 읽을 수 있도록 조정했다.
- `KA 전체` 기본 상태는 대분류를 펼친 뒤 `Infra`·`Application` 중분류까지만 표시하고 세부상품은 접어둔다.
- 상품 현황의 `매출`·`평균매출`·`커버리지` 컬럼은 계층 들여쓰기와 무관하게 같은 x축에 맞춘다. `억원/원`, 제공시점 기준, 전역 상호작용 안내와 중복 하단 설명은 제거했다.
- 좁은 화면에서도 업종·고객명이 서브텍스트에 밀려 사라지지 않도록 보조 문구를 숨기고 식별자 우선으로 표시한다. KPI 카드도 폭에 맞춰 축소된다.
- 고객별 목록은 정렬 순번을 표시하지 않고 고객명만 표시한다.
- 상품 현황·업종별·고객별 표에 공통 컬럼 간격 토큰을 적용해 한쪽 공백이 과도하게 벌어지지 않도록 맞췄다. 100만원 미만 금액은 `백` 단위로 표시한다.
- 공통 토큰을 컴팩트하게 재조정해 매출·평균매출·사용상품 지표 사이의 불필요한 빈 폭을 줄이고, AX/Legacy 칸에 가용 폭을 배분했다.
- 업종·고객 매트릭스 헤더의 보조 문구를 제거하고 `사용상품` 컬럼명을 `전체상품`으로 변경했다.
- `백` 단위로 반올림해 `0.00백`이 되는 초소액은 다시 원 단위로 표시한다.
- 업종별로 펼친 고객 행에서는 상위 업종명이 반복되지 않도록 제거했다.
- 고객명 칼럼은 10자 내외를 우선 확보하고, 계층 들여쓰기·AX/Legacy 영역 폭을 함께 조정했다.
- 고객 상세 KPI도 메인 화면과 같은 밝은 카드·강조색 테마로 통일했다.
- 고객 상세 사용 상품 행의 상품명·금액 간 간격을 줄이고, 추천 로직 안내를 강조 박스와 3줄 설명으로 정리했다.

### 2026-08-07 — `mockup_v2.html` 메인 소스 전환

- `mockup_v2/mockup_v2.html`을 메인 실행 파일로 전환했다. `mockup_v2/mockup_v2.template.html`이 원본 템플릿이고, `build_mockup_v2.mjs`가 CSV 데이터를 내장해 빌드한다.
- `mockup_v2/mockup_v2-vdi.html`은 동일 UI의 VDI 배포본이다. 초기에는 고객·매출·상품 데이터를 비워 두고, 화면의 CSV 업로드로 실데이터를 브라우저에서 적용한다.
- `node mockup_v2/build_mockup_v2.mjs` 한 번으로 메인 더미데이터본과 VDI 배포본을 함께 생성한다. 과거 Option C 호환 파일은 `legacy/`에 보관한다.
- 메인 화면 우측 상단에 `상품분류체계`, `CSV 업로드`, `파일로 저장`을 이식했다. 업로드한 매출 CSV는 현재 대시보드의 모든 집계·추천·비교 화면에 즉시 반영된다.
- 상품분류체계는 AX 대·중분류·상품 중분류·매출명·고객소통명으로 표시하며, M31은 `M2M차량`으로 통합한다.
- 고객 상세에 `동종 업종과 비교`와 `동종 고객과 비교`를 추가했다. 업종 비교는 전체 KA·동종 업종·기준 고객의 침투를, 고객 비교는 2~4개 고객의 상품별 매출 매트릭스를 표시한다.

```bash
node mockup_v2/build_mockup_v2.mjs
node mockup_v2/verify_mockup_v2.mjs
node mockup_v2/verify_mockup_v2_vdi.mjs
```

### 2026-08-07 — 프로젝트 파일 정리

- 현재 메인과 무관한 구버전 메뉴판 파일과 초기 Option C 백업·템플릿은 `legacy/`로 이동했다. 레거시 빌드 체인을 다시 확인할 때는 해당 폴더에서 작업한다.
- `handoff_image/`, `mockup_v3/`, `mockup_variation/`, `schema/` 및 개인·시스템 보조 파일은 삭제했다.
- 현재 메인 빌드에 필요한 데이터는 프로젝트 루트의 매출·월렛쉐어 CSV와 `mockup_v2/` 내부 소스·빌드 파일이다.
- 산출물은 `mockup_v2/mockup_v2.html`(더미데이터본)과 `mockup_v2/mockup_v2-vdi.html`(VDI 배포본)이다.
